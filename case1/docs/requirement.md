# 需求文档：迷你电商「下单 / 优惠券」

> 本文件作为「AI 测试官」场景 B（需求文档 → 验证实现）的 **Markdown 输入**（通用约定格式，无需 JSON fixture）。
> 约定：`## 模块：<相对 sample-app 的源码路径>` 声明后续测试点归属的源码模块；`### 测试点 <id>：<描述>` 声明一个测试点；`关联用例：<用例名子串>` 对该点做 per-point 精确核对（可选）。
> 任意 repo 均可按此约定书写需求，引擎只依赖「模块路径 + 用例名子串」，不绑定任何业务语义。

需求ID: TAPD-100123

## 模块：src/coupon.js
### 测试点 P1：满减券达到门槛立减固定金额，未达门槛则不生效
关联用例：满减券

### 测试点 P2：无门槛立减券直接立减固定金额
关联用例：无门槛立减

### 测试点 P3：折扣券在前面减完后按比例减价（如 9 折 = 减 10%）
关联用例：折扣券

### 测试点 P4：最终应付金额不低于 0（价格不为负）
关联用例：价格不为负

## 模块：src/inventory.js
### 测试点 P5：库存不足时下单失败并回滚已扣库存

## 模块：src/order.js
### 测试点 P6：下单成功返回订单号、原价、应付金额与优惠明细

## 背景说明（非结构化，仅作人类阅读，不参与解析）
- 系统展示商品列表（名称、单价、库存）。
- 优惠券可叠加使用（每类最多 1 张），叠加顺序：满减 → 无门槛 → 折扣。
- 所有金额以「分」为整数单位计算，前端展示时格式化为「¥x.xx」。
- 空购物车不可下单；库存并发扣减当前为单进程内存实现，列为已知风险。
